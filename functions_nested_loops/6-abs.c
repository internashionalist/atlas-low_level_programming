#include "main.h"
/**
 * _abs - computes absolute value of an integer
 * @i: integer to be used
 * Return: absolute value of i
 */

int _abs(int i)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
