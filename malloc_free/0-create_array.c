#include "main.h"

/**
 * create_array - creates char array, initializes with specific char
 * @size: size of array
 * @c: specific char that initializes
 *
 * Return: pointer to the array on success, NULL if it fails
 */

char *create_array(unsigned int size, char c)
{
