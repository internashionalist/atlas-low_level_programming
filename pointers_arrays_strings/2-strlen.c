#include "main.h"

/**
 * _strlen - determines length of a string
 * @s: string
 *
 * Return: length of string
 */

int _strlen(char *s)
{
	int len;

	for (len = 0 ; s[len] ; len++)

	return (len);
}
