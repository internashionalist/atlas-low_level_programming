#include "main.h"

/**
 * factorial - finds factorial of n
 * @n: integer of which to find factorial
 *
 * Return: factorial, or -1 on error
 */

int factorial(int n)
{
	if (n < 0)
	{
		return (-1);
	}

	if (n == 0)
	{
		return (1);
	}
}
