#include <stdio.h>

/**
 * main - Entry point - prints all single digit base 10 numbers 0->
 *
 * Return: Always 0
 */

int main(void)
{
int i;
for (i = 0, i < 10, i++)
{
printf("%i", i);
}
printf("\n");
return (0);
}
